#include<stdio.h>

int small(int array[],int array_size){

int i;
int small=array[0];
        for(i=0;i<n;i++){
            if(small>a[i])
                small=a[i];
        }
return small;
}

        int main(){

        int i,n;
        int *ptr;
        printf("enter number of elements");

        scanf("%d",&n);
        ptr=(int*)malloc(n*sizeof(int));

        if(ptr==NULL)
}
